// src/Popup.js

function Popup() {
  return (
    <div>
      <h3>WhatsApp Extension</h3>
      <button onClick={() => alert('Button clicked!')}>Click me!</button>
    </div>
  );
}

export default Popup;
